export interface TeacherSubject {
    subjectId: string;
    subjectName: string;
    classroomId: string;
    classroomName: string;
  }

  export interface StudentSubject {
    id: string;
    name: string;
    surname: string;
    subjects: string [];
  }